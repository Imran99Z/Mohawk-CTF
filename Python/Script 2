from cryptography.fernet import Fernet
key = Fernet.generate_key()
f = Fernet(key)
token = f.encrypt(b"Nobody told me this CTF was this hard")
token
f.decrypt(token)

print (token)

###Web Version of Token
